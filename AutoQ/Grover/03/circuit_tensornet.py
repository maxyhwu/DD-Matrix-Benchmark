import cudaq
cudaq.set_target('tensornet')
@cudaq.kernel
def circuit():
    qubits = cudaq.qvector(6)
    x(qubits[5])
    h(qubits[0])
    h(qubits[1])
    h(qubits[3])
    h(qubits[5])
    x(qubits[3])
    x(qubits[0])
    x.ctrl([qubits[0], qubits[1]], qubits[2])
    x.ctrl([qubits[2], qubits[3]], qubits[4])
    x.ctrl([qubits[4]], qubits[5])
    x.ctrl([qubits[2], qubits[3]], qubits[4])
    x.ctrl([qubits[0], qubits[1]], qubits[2])
    x(qubits[3])
    x(qubits[0])
    h(qubits[0])
    h(qubits[1])
    h(qubits[3])
    x(qubits[0])
    x(qubits[1])
    x(qubits[3])
    x.ctrl([qubits[0], qubits[1]], qubits[2])
    z.ctrl([qubits[2]], qubits[3])
    x.ctrl([qubits[0], qubits[1]], qubits[2])
    x(qubits[0])
    x(qubits[1])
    x(qubits[3])
    h(qubits[0])
    h(qubits[1])
    h(qubits[3])
    x(qubits[3])
    x(qubits[0])
    x.ctrl([qubits[0], qubits[1]], qubits[2])
    x.ctrl([qubits[2], qubits[3]], qubits[4])
    x.ctrl([qubits[4]], qubits[5])
    x.ctrl([qubits[2], qubits[3]], qubits[4])
    x.ctrl([qubits[0], qubits[1]], qubits[2])
    x(qubits[3])
    x(qubits[0])
    h(qubits[0])
    h(qubits[1])
    h(qubits[3])
    x(qubits[0])
    x(qubits[1])
    x(qubits[3])
    x.ctrl([qubits[0], qubits[1]], qubits[2])
    z.ctrl([qubits[2]], qubits[3])
    x.ctrl([qubits[0], qubits[1]], qubits[2])
    x(qubits[0])
    x(qubits[1])
    x(qubits[3])
    h(qubits[0])
    h(qubits[1])
    h(qubits[3])
    h(qubits[5])

counts = cudaq.sample(circuit, shots_count=1024)
print(counts)