import cudaq
cudaq.set_target('nvidia')
@cudaq.kernel
def circuit():
    qubits = cudaq.qvector(8)
    h(qubits[0])
    h(qubits[1])
    h(qubits[2])
    h(qubits[3])
    h(qubits[4])
    h(qubits[5])
    h(qubits[6])
    h(qubits[7])
    z(qubits[7])
    x.ctrl([qubits[0]], qubits[7])
    x.ctrl([qubits[2]], qubits[7])
    x.ctrl([qubits[4]], qubits[7])
    x.ctrl([qubits[6]], qubits[7])
    h(qubits[0])
    h(qubits[1])
    h(qubits[2])
    h(qubits[3])
    h(qubits[4])
    h(qubits[5])
    h(qubits[6])
    h(qubits[7])

counts = cudaq.sample(circuit, shots_count=1024)
print(counts)