import cudaq
cudaq.set_target('tensornet')
@cudaq.kernel
def circuit():
    qubits = cudaq.qvector(14)
    x(qubits[0])
    x(qubits[2])
    x(qubits[4])
    x(qubits[6])
    x.ctrl([qubits[0], qubits[1]], qubits[8])
    x.ctrl([qubits[2], qubits[8]], qubits[9])
    x.ctrl([qubits[3], qubits[9]], qubits[10])
    x.ctrl([qubits[4], qubits[10]], qubits[11])
    x.ctrl([qubits[5], qubits[11]], qubits[12])
    z.ctrl([qubits[12]], qubits[6])
    x.ctrl([qubits[5], qubits[11]], qubits[12])
    x.ctrl([qubits[4], qubits[10]], qubits[11])
    x.ctrl([qubits[3], qubits[9]], qubits[10])
    x.ctrl([qubits[2], qubits[8]], qubits[9])
    x.ctrl([qubits[0], qubits[1]], qubits[8])
    x(qubits[0])
    x(qubits[2])
    x(qubits[4])
    x(qubits[6])
    h(qubits[0])
    h(qubits[1])
    h(qubits[2])
    h(qubits[3])
    h(qubits[4])
    h(qubits[5])
    h(qubits[6])
    x(qubits[0])
    x(qubits[1])
    x(qubits[2])
    x(qubits[3])
    x(qubits[4])
    x(qubits[5])
    x(qubits[6])
    x.ctrl([qubits[0], qubits[1]], qubits[8])
    x.ctrl([qubits[2], qubits[8]], qubits[9])
    x.ctrl([qubits[3], qubits[9]], qubits[10])
    x.ctrl([qubits[4], qubits[10]], qubits[11])
    x.ctrl([qubits[5], qubits[11]], qubits[12])
    z.ctrl([qubits[12]], qubits[6])
    x.ctrl([qubits[5], qubits[11]], qubits[12])
    x.ctrl([qubits[4], qubits[10]], qubits[11])
    x.ctrl([qubits[3], qubits[9]], qubits[10])
    x.ctrl([qubits[2], qubits[8]], qubits[9])
    x.ctrl([qubits[0], qubits[1]], qubits[8])
    x(qubits[0])
    x(qubits[1])
    x(qubits[2])
    x(qubits[3])
    x(qubits[4])
    x(qubits[5])
    x(qubits[6])
    h(qubits[0])
    h(qubits[1])
    h(qubits[2])
    h(qubits[3])
    h(qubits[4])
    h(qubits[5])
    h(qubits[6])
    z(qubits[7])

    counts = cudaq.sample(circuit, shots_count=1024)
    print(counts)